// Functional component representing the Header of the application
function Header() {
  return (
    <div>
      {/* Display a heading in the Header */}
      <h2>This is my Header in another component!</h2>
    </div>
  );
}

// Export the Header component to make it available for use in other parts of the application
export default Header;
